# Root-level environment variables for the monorepo.
# App-specific variables live next to each app:
#   - client/.env.example
#   - server/.env.example
# Copy an .env.example to .env in the relevant workspace and fill in real values.
